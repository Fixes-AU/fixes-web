import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Register as a Tradie | Fixes",
  description: "Join Australia's fastest-growing tradie marketplace. Create your Fixer profile, get matched with local jobs, and start earning with secure payments and flexible scheduling.",
}

export default function RegisterTradieLayout({ children }: { children: React.ReactNode }) {
  return children
}
