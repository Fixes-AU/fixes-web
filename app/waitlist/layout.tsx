import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Join the Waitlist | Fixes",
  description: "Be first in line when Fixes launches in your area. Join the waitlist for early access to Australia's AI-powered tradie marketplace.",
}

export default function WaitlistLayout({ children }: { children: React.ReactNode }) {
  return children
}
